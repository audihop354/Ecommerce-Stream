from pipeline_app.spark_pipeline.definitions import (
    RawDatasetSpec,
)
from pipeline_app.spark_pipeline.transforms import (
    build_customers_projection,
    build_incidents_projection,
    build_marketing_spend_projection,
    build_order_items_projection,
    build_orders_projection,
    build_payments_projection,
    build_products_projection,
    build_support_tickets_projection,
    enrich_customers_for_landing,
    enrich_incidents_for_landing,
    enrich_marketing_spend_for_landing,
    enrich_order_items_for_landing,
    enrich_orders_for_landing,
    enrich_payments_for_landing,
    enrich_products_for_landing,
    enrich_support_tickets_for_landing,
)


RAW_DATASET_SPECS = [
    RawDatasetSpec(
        label="customers",
        topic_suffix="customers",
        raw_prefix="raw/customers/",
        checkpoint_name="customers",
        projection_builder=build_customers_projection,
        enrich_builder=enrich_customers_for_landing,
    ),
    RawDatasetSpec(
        label="products",
        topic_suffix="products",
        raw_prefix="raw/products/",
        checkpoint_name="products",
        projection_builder=build_products_projection,
        enrich_builder=enrich_products_for_landing,
    ),
    RawDatasetSpec(
        label="orders",
        topic_suffix="orders",
        raw_prefix="raw/orders/",
        checkpoint_name="orders",
        projection_builder=build_orders_projection,
        enrich_builder=enrich_orders_for_landing,
    ),
    RawDatasetSpec(
        label="order_items",
        topic_suffix="order_items",
        raw_prefix="raw/order_items/",
        checkpoint_name="order_items",
        projection_builder=build_order_items_projection,
        enrich_builder=enrich_order_items_for_landing,
    ),
    RawDatasetSpec(
        label="payments",
        topic_suffix="payments",
        raw_prefix="raw/payments/",
        checkpoint_name="payments",
        projection_builder=build_payments_projection,
        enrich_builder=enrich_payments_for_landing,
    ),
    RawDatasetSpec(
        label="support_tickets",
        topic_suffix="support_tickets",
        raw_prefix="raw/support_tickets/",
        checkpoint_name="support_tickets",
        projection_builder=build_support_tickets_projection,
        enrich_builder=enrich_support_tickets_for_landing,
    ),
    RawDatasetSpec(
        label="incidents",
        topic_suffix="incidents",
        raw_prefix="raw/incidents/",
        checkpoint_name="incidents",
        projection_builder=build_incidents_projection,
        enrich_builder=enrich_incidents_for_landing,
    ),
    RawDatasetSpec(
        label="marketing_spend",
        topic_suffix="marketing_spend",
        raw_prefix="raw/marketing_spend/",
        checkpoint_name="marketing_spend",
        projection_builder=build_marketing_spend_projection,
        enrich_builder=enrich_marketing_spend_for_landing,
    ),
]